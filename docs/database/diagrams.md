# Data Structures

> Auto-generated by repo-map. Last updated: 2026-02-17.

This project is an embedded firmware (ESP32) with no persistent database. Data structures exist only in RAM as C++ structs. This document maps the runtime data model.

---

## Runtime Data Model

```mermaid
erDiagram
    FesParameters {
        int fes_duration_s "Stimulation duration (seconds)"
        int pulse_width_us "Biphasic pulse width (microseconds)"
        float frequency "Pulse frequency (Hz)"
        float amplitude "Output voltage (V, via potentiometer)"
    }

    SemgParameters {
        float gain "Signal amplification factor"
        float difficulty "Trigger difficulty (1-100%)"
        float threshold "Computed trigger threshold"
    }

    SessionStatus {
        short complete_stimuli_amount "Successful stimulations"
        short interrupted_stimuli_amount "Interrupted stimulations"
        bool paused "Session paused flag"
        bool ongoing "Session active flag"
        uint32 time_of_last_trigger "Last trigger timestamp (ms)"
        uint32 session_duration "Total session time (ms)"
    }

    SessionParameters {
        short maximumStimuliAmount "Max stimulations before auto-stop"
        short minimumSecondsBetweenStimuli "Cool-down between stimuli"
        uint32 maximum_duration "Max session duration (ms)"
        bool limited_by_duration "Duration-limited mode"
        bool limited_by_amount_of_trigger "Count-limited mode"
    }

    BinaryPacketHeader {
        uint8 magic "Start marker 0xAA"
        uint8 message_code "Always 0x0D (13)"
        uint32 timestamp "millis() value"
        uint16 sample_count "Samples in payload (50)"
    }

    SessionStatus ||--o{ FesParameters : "uses"
    SessionStatus ||--o{ SemgParameters : "uses"
    SessionStatus ||--o{ SessionParameters : "configured by"
    BinaryPacketHeader ||--|{ SemgParameters : "streams from"
```

---

## Circular Buffer Architecture

```mermaid
erDiagram
    AdcCircularBuffer {
        int16 buffer_512 "512 x int16_t samples"
        int write_index "Producer pointer (ADC task)"
        int read_index "Consumer pointer (streaming task)"
        int available_samples "Queued sample count"
    }

    SemgStreamingBuffer {
        int16 buffer_300 "300 x int16_t samples"
        int buffer_write_index "Write pointer"
        int buffer_read_index "Read pointer"
        bool streaming_active "Streaming state flag"
    }

    AdcCircularBuffer ||--|| SemgStreamingBuffer : "feeds"
```
