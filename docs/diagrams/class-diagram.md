# Class / Module Diagram

> Auto-generated by repo-map. Last updated: 2026-02-17.

All classes use the **static singleton pattern** (deleted constructors) except `Led`, `Battery`, `NotchFilter`, and `Potentiometer`.

---

## Module Dependency Map

```mermaid
classDiagram
    class MessageHandler {
        +init()
        +start()
        +loop(void*)
        +sendMessage(DynamicJsonDocument*)
        -interpretMessage(String)
        -handleGyroscopeMessage()
        -handleSessionParametersMessage()
        -sendAck(int)
        -message_handler_queue : QueueHandle_t
        +task_handle : TaskHandle_t
    }

    class Session {
        +start()
        +stop()
        +pauseFromSession()
        +pauseFromMessageHandler()
        +resume()
        +singleStimulus()
        +sendSessionStatus()
        +detectionAndStimulation()
        +loop(void*)
        +task_handle : TaskHandle_t
        +status : SessionStatus
        -parameters : SessionParameters
    }

    class Semg {
        +init()
        +isTrigger() bool
        +enableStreaming()
        +disableStreaming()
        +isStreaming() bool
        +streamingTask(void*)
        +setDifficulty(int)
        +testTrigger(int)
        +parameters : SemgParameters
        -streaming_buffer : int16_t[]
        -streaming_active : bool
        -samplingTimer : TimerHandle_t
    }

    class Fes {
        +init()
        +fesLoop()
        +begin()
        +stopFes()
        +setParameters(int,int,double)
        +increaseAmplitude(int)
        +decreaseAmplitude(int)
        +emergency_stop : bool
        +stimulating : bool
        +parameters : FesParameters
        -potentiometer : Potentiometer
    }

    class Adc {
        +init()
        +getValue(int) float
        +startContinuousMode(int)
        +stopContinuousMode()
        +hasNewSample() bool
        +getLastSample() int16_t
        -ads : Adafruit_ADS1115
        -circular_buffer : int16_t[512]
        -adc_task_handle : TaskHandle_t
    }

    class Bluetooth {
        +init()
        +readData() String
        +sendData(String) bool
        +sendRawData(uint8_t*,size_t) bool
        +isConnected() bool
        +waitForConnection()
    }

    class Gyroscope {
        +init()
        +getLastValue() float
        +sendLastValue()
        +calibrateMPU6050()
        +calculatePitch() float
        +gyroscopeRoutine() float
        -mpu : Adafruit_MPU6050
    }

    class Potentiometer {
        +init()
        +pulse(int)
        +increase(int)
        +decrease(int)
        +voltageSet(float) float
        +getCorrectedVoltage() float
    }

    class SemgFilter {
        +filter(float) float
        +filterWithNotch(float) float
        +updateSamplingRate(float,int,int,bool)
        +resetState()
        -high_pass : Filter
        -low_pass : Filter
        -notch_60hz : NotchFilter
    }

    class NotchFilter {
        +filterIn(float) float
        +flush()
        +setNotchFrequency(float,bool)
        +setBandwidth(float,bool)
        +setSampleRate(float,bool)
    }

    class Led {
        +set(bool)
        +toggle()
        +blink(int,int)
        +turnOnFor(int)
        -pin : int
        -status : bool
    }

    class EmergencyButton {
        +init()
        +stop()
        +emergencyStop() IRAM_ATTR
    }

    class Battery {
        +isLow() bool
        +updateBatteryLevel() double
        -adc_input : int
        -threshold : double
    }

    class Battery_monitor {
        -stimuli_battery : Battery
        -main_battery : Battery
    }

    MessageHandler --> Bluetooth : reads/writes
    MessageHandler --> Session : start/stop/pause/resume
    MessageHandler --> Semg : streaming control
    MessageHandler --> Fes : single stimulus
    MessageHandler --> Gyroscope : read angle
    MessageHandler --> Potentiometer : set voltage

    Session --> Semg : isTrigger()
    Session --> Fes : fesLoop()
    Session --> EmergencyButton : ISR binding

    Semg --> Adc : continuous mode sampling
    Semg --> SemgFilter : bandpass + notch
    Semg --> Fes : references (shared timer)
    Semg --> Led : trigger indicator

    SemgFilter --> NotchFilter : 60 Hz rejection

    Fes --> Potentiometer : amplitude control
    Fes --> Led : FES indicator

    Battery_monitor --> Battery : monitoring
    Battery --> Adc : voltage reading

    Gyroscope --> Adc : shares I2C bus
```

---

## FreeRTOS Task Map

```mermaid
flowchart LR
    subgraph "Core 0 (session_cpu)"
        T1["Session::loop\nPriority: 20\nStack: 4096"]
    end

    subgraph "Core 1 (secondary_cpu)"
        T2["MessageHandler::loop\nPriority: 20\nStack: 4096"]
        T3["Adc::adcTaskLoop\nPriority: 18\nStack: 2048"]
        T4["Semg::streamingTask\nPriority: 10\nStack: 4096"]
    end

    T2 -.->|"controls"| T1
    T3 -.->|"feeds samples"| T4
    T1 -.->|"reads ADC"| T3
```

---

## Hardware Peripheral Map

```mermaid
flowchart TD
    subgraph "I2C Bus (SDA=21, SCL=22)"
        ADS[ADS1115 ADC\n0x48\n860 SPS]
        MPU[MPU6050 IMU\n0x68]
    end

    subgraph "SPI-like (Bit-bang)"
        POT[Digital Potentiometer\nCS=13, INC=14, UD=12]
    end

    subgraph "GPIO Output"
        HB1[H-Bridge IN1 = 32]
        HB2[H-Bridge IN2 = 33]
        LED_P[LED Power = 27]
        LED_T[LED Trigger = 26]
        LED_F[LED FES = 25]
        SEMG_EN[sEMG Enable = 18]
    end

    subgraph "GPIO Input"
        EMR[Emergency Button = 35]
        BAT_S[Stimuli Battery = ADC3]
        BAT_M[Main Battery = ADC2]
    end

    subgraph "UART2 (9600 baud)"
        BT[HC-05/HC-06 Bluetooth\nRX=16, TX=17\nStatus=23]
    end
```
