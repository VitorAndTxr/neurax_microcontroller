# Python dependencies for sEMG data capture and analysis

# Required for serial communication
pyserial>=3.5

# Required for COM port detection (Windows/Linux/Mac)
pyserial-win32  # Windows only, auto-installs with pyserial

# Optional: For data visualization
matplotlib>=3.5.0
pandas>=1.3.0
numpy>=1.21.0

# Optional: For advanced signal processing
scipy>=1.7.0
